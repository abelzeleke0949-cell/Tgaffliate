# Backend API base URL (must include /api)
VITE_API_URL=http://localhost:5001/api

# Telegram bot username (no @, as registered with BotFather) — used to build the
# t.me/<username>?start=... affiliate links generated in the Mini App's Influencer mode
VITE_TELEGRAM_BOT_USERNAME=your_bot_username
